package org.springhibernate.controllers;

import org.springframework.stereotype.Controller;

/**
 *
 */
@Controller
public class TeamSelectController {

    /*
     * @RequestMapping(value = "/SpringHibernateSelectTeam", method = RequestMethod.POST) public
     * String selectTeam(@ModelAttribute("customers") Customer cust, BindingResult
     * result, @ModelAttribute("user1") User user1, HttpServletRequest request) { return "Login";
     * 
     * }
     */

    /*
     * @RequestMapping(value = "/SelectTeam", method = RequestMethod.GET) public String
     * selectTeamGet(@ModelAttribute("customers") Customer cust, BindingResult result,
     * 
     * @ModelAttribute("user1") User user1, HttpServletRequest request) { return "Login";
     * 
     * }
     * 
     * public String selectTeams(@ModelAttribute("customers") Customer cust, BindingResult
     * result, @ModelAttribute("user1") User user1, HttpServletRequest request, HttpServletResponse
     * response, WebRequest webRequest, Locale locale, ZoneId zone, Reader reader, Writer writer,
     * HttpMethod method, Principal principal, @PathVariable("OwnerId") String Owner,) { return
     * "Login";
     * 
     * }
     */
}
